/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // output: 'export',
  distDir: 'all-inkl',
 
  // images: {
  //   unoptimized: true,
  // },
};

module.exports = nextConfig;
